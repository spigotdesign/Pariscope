<?php if ( !class_exists('FLBuilderModel') || (class_exists('FLBuilderModel') && !FLBuilderModel::is_builder_enabled()) ): ?>
			
	<header class="page-header">

		This is the page header stuff
		
	</header>

<?php endif; ?>